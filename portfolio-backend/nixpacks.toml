[phases.setup]
nixPkgs = ['nodejs-18_x', 'npm-9_x']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "Skipping TypeScript build - using ts-node"']

[start]
cmd = 'npx ts-node src/server.ts'
